"""SQLAlchemy ORM models.

Empty placeholder in this commit. Patient, Treatment, Medication, and
AuditLogEntry land in the next commit. The module is created now so
alembic/env.py's forward import resolves cleanly.
"""
